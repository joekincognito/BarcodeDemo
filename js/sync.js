//inventory - check timestamp

//orders - compare device order timestamp to database order timestamp
/* if (deviceOrderTime > dateOrderTime)
	{
		$.post device order info
	}
	elseif (dateOrderTime > deviceOrderTime)
	{
		if (manager)
			$.get ALL database order info
		else
			$.get database order info for inventory center
	}
	else
	{
		the times match...all is good
	}
*/